Задание 1 Напишите программу, которая принимает на вход вещественное число
# и показывает сумму его цифр

x = input('Введите число ')


def summa(x):  # Функция нахождения суммы чисел в заданном числе
     if float(x) < 0:  # Проверяем  знак перед числом
          x = float(x) * (-1)
     number = 0

     for i in str(x):
          if i != '.':           # Проверяем  раделитель дробной части (.)
               number += int(i)

     return number


print(f'Сумма чисел равна {summa(x)}')

